---
name: hello-world
description: A minimal example skill that demonstrates the basic structure of a skill. Use when learning how skills work or as a starting template for a new skill.
---

# Hello World

This is the simplest possible skill — just a SKILL.md with frontmatter and a few lines of guidance.

## What This Demonstrates

- The required YAML frontmatter with `name` and `description`
- A `description` that follows the routing template: capability + "Use when" + context
- A minimal body with clear, concise instructions

## Usage

When the user says "hello" or wants a quick greeting, respond with a friendly message.

### Steps

1. Greet the user warmly
2. Offer to help with their next task
